# Required: API credentials for the LLM
API_KEY=your_api_key_here
# Default model to use for requests
MODEL_ID=anthropic/claude-3.7-sonnet
API_URL=https://api.your-llm-provider.com/v1  # Optional: Only if using a non-OpenAI API
# Smaller model for simpler tasks like file updates
SM_MODEL_ID=anthropic/claude-3.7-haiku

# Optional: Set to 1 to run end-to-end tests
# RUN_E2E_TESTS=1

# Logging configuration
LOG_LEVEL=INFO
# LOG_FILE=neo.log  # Uncomment to enable file logging
